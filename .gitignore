# .NET
bin/
obj/
.vs/
*.user

# build.sh ready-to-run output
out/

# Rust
target/

# CUDA / native build outputs
**/build/
*.o
*.so

# Windows native build outputs (build.ps1 / CMake)
**/build-win/
*.obj
*.dll
*.lib
*.exp
*.pdb

# CUTLASS is a git submodule (see .gitmodules). The gitlink is committed so that
# `git clone --recurse-submodules` fetches it; its working-tree files are not vendored.

# ── Public-release exclusions (added 2026-06-16) ───────────────
# Prebuilt binaries, archives, and backups — this is a SOURCE release.
*.exe
*.so.*
*.dylib
*.zip
*.7z
*.tar
*.tar.gz
*.bak
*.bak-*
*.bak*

# Prebuilt / staged packages & per-die test folders (local only)
/a750 test/
/b580 gold/
/b70 test/
/a750-package/
/kryptex-package/
/stratum-package/

# Personal / internal notes — never publish
.claude/
message.txt
reply-draft.md
pool compatibility.txt
A1start.bat
A2start-alphapool.bat

# IDE
.idea/
.vscode/
*.swp

